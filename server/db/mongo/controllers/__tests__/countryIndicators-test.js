/*
import expect from 'expect';
import mongoose from 'mongoose';
import sinon from 'sinon';
import 'sinon-mongoose'
import chai from 'chai';
import chaiHttp from 'chai-http';
// import { listForCountry } from '../countryIndicators';
// import CountryIndicatorInfo from '../../models/countryIndicatorInfo';
import app from '../../../../';

let CountryIndicatorInfo;
//const app = 'http://localhost:3001';

chai.use(chaiHttp);


describe('countryIndicators controller', () => {
  describe('GET /:country', () => {
    it('returns indicators associated to the requested country', (done) => {
      chai.request(app).get('/united-states')
        .end((err, res) => {
          expect(err).toNotExist();
          expect(res).toEqual('asdf');
          done();
        });

      // const country = 'united-states';
      // const req = {
      //  params: {
      //    country: 'united-states'
      //  }
      // };

      // const InfoMock = sinon.mock(CountryIndicatorInfo);
      // const expectedResult = [];
      // InfoMock.expects('find').withArgs({country}).yields(null, expectedResult);
      // listForCountry(req, res);
      // CountryIndicatorInfo.find(function (err, result) {
      //     TodoMock.verify();
      //     TodoMock.restore();
      //     expect(result.status).to.be.true;
      //     done();
      // });
    });
  });
});
*/
